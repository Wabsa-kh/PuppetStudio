# Security policy

Puppet Studio is an alpha. Report security issues privately to the repository owner through GitHub rather than posting tokens or private project files in a public issue.

The optional control server binds only to `127.0.0.1` and uses a newly generated token each time it starts. Do not share that token. Project files embed artwork; only open `.puppet` files from people you trust.
